
// There's not actually a great deal of testing we can do for scancode.hpp
// -- it just contains a single (giant) enum, with values taken directly from
// the C version.
//
// So we'll just include the header so that the compiler scans it an makes sure
// we're not doing anything too silly.

#include <sdl++/scancode.hpp>
